# 「无线调试」统一面板功能介绍

> 适用版本：Super_ADB Main 2026-08-09+
> 模块位置：`Super_ADB_Main/dialogs/wireless_debug_dialog.py`
> 子模块：`Super_ADB_Main/dialogs/lan_scanner_dialog.py`、`Super_ADB_Main/dialogs/wifi_pair_dialog.py`

---

## 一、功能概览

把原来主界面「设备连接」区的两个入口——**「局域网扫描」**和**「WiFi 配对」**——合并成一个弹窗，用 `QTabWidget` 分两个标签页：

- 减少主窗口按钮数量，入口更清晰。
- 两个工具共用一套窗口生命周期管理（关闭时统一停止后台线程）。
- 后续若再增加无线调试能力（如二维码发现、历史记录等），可继续扩展为第三个标签页。

---

## 二、入口与触发

- **位置**：主窗口「设备连接」区，原本的「局域网扫描」「WiFi 配对」两个按钮合并为 **「无线调试」** 按钮。
- **行为**：点击后弹出「无线调试」弹窗，默认停留在上一次关闭时的标签页。
- **重复点击**：窗口已开就 `raise_()` + `activateWindow()` 前置，不重复开。

---

## 三、界面布局

<p align="center">
  <img src="wireless-debug-pair.png" width="560" alt="无线调试 - 配对码连接页"/>
</p>
<p align="center">
  <img src="wireless-debug-lan.png" width="560" alt="无线调试 - 局域网扫描页"/>
</p>

弹窗顶部为一句话说明，中间是两个标签页：

### 标签页 1：局域网扫描

- 自动检测本机 IP，默认扫描同网段 `192.168.x.0/24`。
- 可自定义 IP 范围、超时、并发线程数、探测端口。
- 发现设备后可一键连接或复制 IP。
- 详细用法见 [lan-scanner-guide.md](lan-scanner-guide.md)。

### 标签页 2：配对码连接

- 输入 IP、配对端口、6 位配对码（或一键粘贴 / 扫码）。
- 配对成功后自动连接调试端口（默认 5555）。
- 已配对设备自动保存，可一键重连。
- 配对 / 连接历史可导出 CSV / JSON。
- 详细用法见 [wifi-pair-guide.md](wifi-pair-guide.md)。

---

## 四、典型使用流程

### 场景 1：新手机首次无线调试

1. 手机：设置 → 开发者选项 → 无线调试 →「使用配对码配对设备」。
2. 电脑：主窗口点 **「无线调试」** → 切到 **「配对码连接」** 页。
3. 填入手机弹窗中的 IP、配对端口、配对码 → 点「开始配对」。
4. 配对成功后点「连接调试端口」（默认 5555），主窗口设备列表自动刷新。

### 场景 2：不知道设备 IP

1. 主窗口点 **「无线调试」** → 切到 **「局域网扫描」** 页。
2. 保持默认网段，点「开始扫描」。
3. 在结果里找到目标设备，点「连接」。

---

## 五、注意事项

- 嵌入子对话框后，其 `closeEvent` 不会单独触发；关闭「无线调试」弹窗时会统一调用两个子对话框的 `cleanup()`，停掉后台扫描 / 配对 / 连接线程，避免悬挂。
- 配对端口（如 `38973`）是一次性的，配对完成后要立即连接调试端口；等待过久会过期。
- 历史记录与已配对设备分别保存在 `wifi_debug_history.json` 和 `wifi_paired_devices.json`（项目配置目录）。

---

## 六、变更记录

- **2026-08-09**：随 commit `e657c0a` 新增「无线调试」统一面板，并补齐本文档与截图。
