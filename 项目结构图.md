# Super_ADB 项目结构图

> 更新于 2026-08-09。按功能模块归类整理后的目录结构，**并补充「启动内存优化」一节**（本次对启动占用的三项优化）。

## 目录树

```
Super_ADB/
├── README.md                      # 项目说明文档
├── requirements.txt               # 运行依赖（PySide6 + Pillow）
├── 项目结构图.md                  # 本文件
├── LICENSE                        # 许可证
│
├── ui/                            # UI 设计源文件
│   ├── Super_ADB.ui              # 主窗口布局（Qt Designer）
│   ├── png.qrc                    # 图标资源定义（⚠️已精简：仅保留应用图标）
│   ├── Super_ADB.png             # 应用图标（512×512，编译进 png_rc）
│   ├── Super_ADB.ico             # 打包用图标
│   └── 公众号.jpg / 透明公众号.png # 公众号二维码源素材（⚠️已移出资源，运行时从磁盘按需加载）
│
├── Super_ADB_Main/               # 主程序目录
│   ├── Super_ADB_Main.py         # 主入口 / 主窗口逻辑 / 信号接线
│   │                             #   （⚠️重型子模块改为「用到才 import」，启动不再全量加载）
│   ├── Super_ADB.py              # uic 从 .ui 自动生成（⚠️勿手改）
│   ├── Super_ADB.spec            # PyInstaller 打包规格
│   ├── 界面样式.py                # 深色主题样式表（STYLE_SHEET / FONT_FAMILY）
│   ├── popup_style.py            # 弹窗高亮样式（add_green_glow 等）
│   ├── adb_utils.py              # ADB 命令封装层（AdbHelper/AdbDeviceOps/AdbFileManager）
│   ├── adb_shell_config.json     # 配置（窗口几何 / shell 命令）
│   ├── png_rc.py                 # 编译后的资源文件（⚠️已重编译：614KB → 55KB）
│   ├── resources/                # 运行时磁盘加载资源
│   │   └── 公众号.jpg            # 关于弹窗二维码，运行时按需从磁盘读取（不占资源内存）
│   │
│   ├── dialogs/                  # 【弹窗模块】⚠️全部改为「打开时才 import」
│   │   ├── about_dialog.py       # 关于（二维码 / 版本 / 反馈；二维码从磁盘加载）
│   │   ├── install_zip_dialog.py  # 安装 / 解包（拖入 APK·ZIP 预览 + adb install）
│   │   ├── json_tool_dialog.py   # JSON 工具（格式化/压缩/差异/YAML/Schema/树）
│   │   ├── md5_dialog.py         # MD5 多算法校验 + 进度 + 导出 + 右键集成
│   │   ├── timestamp_dialog.py   # 时间戳 ↔ 北京时间 转换
│   │   ├── hash_context_menu.py   # 右键「计算哈希」独立入口
│   │   ├── monkey_runner_window.py # Monkey 压测窗口
│   │   ├── wireless_debug_dialog.py # 无线调试（扫码/手动配对）
│   │   ├── wifi_dialog.py         # Wi-Fi 调试引导
│   │   └── tcpdump_dialog.py      # tcpdump 抓包窗口
│   │
│   ├── pages/                    # 【主窗口子页面】（静态分屏常驻，启动即建）
│   │   ├── log_viewer_page.py    # 日志抓取（QProcess 流式 logcat）
│   │   ├── file_manager_page.py  # 文件管理
│   │   └── capture_screenshot.py # 截图（PIL.ImageGrab）
│   │
│   ├── monitors/                 # 【性能监控】⚠️改为「打开监控窗口时才 import」
│   │   ├── device_perf_monitor.py # 设备级监控（独立窗口 + 采样线程）
│   │   └── app_perf_monitor.py    # 应用级监控（12 项指标 / 泄漏 / ANR / OOM / 报告）
│   │
│   ├── utils/                    # 【工具模块】
│   │   ├── axml_decoder.py       # Android Binary XML 解码
│   │   ├── cert_parser.py        # 证书解析
│   │   └── fav_combo.py          # 可收藏下拉框控件（FavComboBox）
│   │
│   └── scripts/                  # 【构建 / 验证脚本】（不参与打包运行）
│       ├── generate_icon.py      # 生成应用图标（PIL）
│       └── trace_mem.py          # 启动内存实测脚本（P2：量化延迟 import 收益）
│
├── feature_intro/                # 功能介绍文档（HTML / PNG / MD）
│   ├── *.html                    # 功能介绍页
│   ├── *.png                     # 截图
│   └── *.md                      # 各模块说明（含 md5-check / json-tool / timestamp 等）
│
├── build/                        # PyInstaller 构建中间产物
├── dist/                         # 打包输出
└── generated-videos/             # 介绍视频产物
```

## 模块职责速查

| 目录 / 文件 | 职责 |
|---|---|
| `Super_ADB_Main.py` | 主窗口、无边框拖拽、托盘、所有按钮信号接线、子目录 `sys.path` 注入 |
| `adb_utils.py` | ADB 命令封装：设备列表、shell 执行、文件推拉、包管理 |
| `ui/Super_ADB.ui` → `Super_ADB.py` | UI 定义与自动生成代码（改 UI 必须走 uic） |
| `dialogs/` | 各独立弹窗，复用窗口模式（重复点击 `raise_` 置顶）；**启动不加载，用到才 import** |
| `pages/` | 嵌入主窗口的静态分屏页面（日志 / 文件 / 截图），启动即建 |
| `monitors/` | 后台线程采样的性能监控，ScrollChart 自绘图表；**启动不加载，用到才 import** |
| `utils/` | 无 UI 依赖的纯逻辑模块（解码 / 解析 / 控件） |
| `scripts/` | 不参与运行的构建/验证脚本（含 `trace_mem.py`） |

## 依赖关系要点

- 所有模块**扁平裸 import**，无包结构；`Super_ADB_Main.py` 启动时把 `dialogs/ pages/ monitors/ utils/` 加入 `sys.path`。
- **启动加载策略（2026-08-09 优化后）**：顶层只保留 `file_manager_page` / `log_viewer_page` / `popup_style` 及核心依赖；`device_perf_monitor` / `monkey_runner_window` / `app_perf_monitor` / `install_zip_dialog` / `about_dialog` / `json_tool_dialog` / `md5_dialog` / `timestamp_dialog` / `wireless_debug_dialog` / `wifi_dialog` / `tcpdump_dialog` 共 **11 个重型模块改为在对应 `open_xxx` 方法内局部 import**，启动阶段完全不加载（实测 import 主窗口后这 11 个模块均不在 `sys.modules`）。
- 高引用文件留在根目录（`界面样式.py` / `popup_style.py` / `adb_utils.py` / `png_rc.py`），避免大规模改 import。
- `Super_ADB.py` 与 `png_rc.py` 由工具链生成，**禁止手改**。

## 启动内存优化（2026-08-09）

| 级别 | 优化项 | 做法 | 实测收益 |
|---|---|---|---|
| P0 | 延迟 import 重型子模块 | 11 个弹窗/监控模块由顶层 import 改为 `open_xxx` 内局部 import | import 主窗口阶段省下 ~16.9 MB（11 模块 eager 加载合计增量）；启动不再加载 matplotlib/numpy 等重型依赖 |
| P0 | 资源文件瘦身 | `ui/png.qrc` 仅保留应用图标，公众号二维码移出资源、改为运行时从 `Super_ADB_Main/resources/公众号.jpg` 磁盘加载；重编译 `png_rc.py` | `png_rc.py` 614 KB → 55 KB；公众号图片字节不再常驻资源内存 |
| P1 | 收敛线程池上限 | `QThreadPool().setMaxThreadCount(6)` → `min(cpu_count, 4)` | 每线程约 1 MB 栈常驻，省 ~2 MB，后台任务并发仍够用 |
| P2 | 内存实测脚本 | 新增 `Super_ADB_Main/scripts/trace_mem.py`（offscreen + tracemalloc + RSS 分阶段测量） | 量化上述优化、便于回归对比 |

> 说明：PySide6 框架本身的 shiboken 绑定层（加载整个 Qt 符号）属固有常驻开销（150–400 MB），不在本项目可优化范围内；本次优化针对「项目自身」的可降占用。实测（`trace_mem.py`，无设备 offscreen）主窗口构造后 RSS ≈ 83.6 MB，重型子模块改按需加载后启动阶段不再额外占用约 16.9 MB。

## 返回 README

详见 [`README.md`](README.md)。
