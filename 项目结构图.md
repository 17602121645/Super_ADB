# Super_ADB 项目结构图

> 更新于 2026-08-08，按功能模块归类整理后的目录结构。

## 目录树

```
Super_ADB/
├── README.md                      # 项目说明文档
├── requirements.txt               # 运行依赖（PySide6 + Pillow）
├── 项目结构图.md                  # 本文件
├── LICENSE                        # 许可证
│
├── ui/                            # UI 设计源文件
│   ├── Super_ADB.ui              # 主窗口布局（Qt Designer）
│   ├── png.qrc                    # 图标资源定义
│   └── Super_ADB.png             # 应用图标（512×512）
│
├── Super_ADB_Main/               # 主程序目录
│   ├── Super_ADB_Main.py         # 主入口 / 主窗口逻辑 / 信号接线
│   ├── Super_ADB.py              # uic 从 .ui 自动生成（⚠️勿手改）
│   ├── 界面样式.py                # 深色主题样式表（STYLE_SHEET / FONT_FAMILY）
│   ├── popup_style.py            # 弹窗高亮样式（add_green_glow 等）
│   ├── adb_utils.py              # ADB 命令封装层（AdbHelper/AdbDeviceOps/AdbFileManager）
│   ├── adb_shell_config.json     # 配置（窗口几何 / shell 命令）
│   ├── png_rc.py                 # 编译后的资源文件（⚠️勿手改）
│   │
│   ├── dialogs/                  # 【弹窗模块】
│   │   ├── about_dialog.py       # 关于（二维码 / 版本 / 反馈）
│   │   ├── install_zip_dialog.py  # 安装 / 解包（拖入 APK·ZIP 预览 + adb install）
│   │   ├── json_tool_dialog.py   # JSON 工具（格式化/压缩/差异/YAML/Schema/树）
│   │   ├── md5_dialog.py         # MD5 多算法校验 + 进度 + 导出 + 右键集成
│   │   ├── timestamp_dialog.py   # 时间戳 ↔ 北京时间 转换
│   │   ├── hash_context_menu.py   # 右键「计算哈希」独立入口
│   │   ├── monkey_runner_window.py # Monkey 压测窗口
│   │   └── tcpdump_dialog.py     # tcpdump 抓包窗口
│   │
│   ├── pages/                    # 【主窗口子页面】
│   │   ├── log_viewer_page.py    # 日志抓取（QProcess 流式 logcat）
│   │   ├── file_manager_page.py  # 文件管理
│   │   └── capture_screenshot.py # 截图（PIL.ImageGrab）
│   │
│   ├── monitors/                 # 【性能监控】
│   │   ├── device_perf_monitor.py # 设备级监控（独立窗口 + 采样线程）
│   │   └── app_perf_monitor.py    # 应用级监控（12 项指标 / 泄漏 / ANR / OOM / 报告）
│   │
│   ├── utils/                    # 【工具模块】
│   │   ├── axml_decoder.py       # Android Binary XML 解码
│   │   ├── cert_parser.py        # 证书解析
│   │   └── fav_combo.py          # 可收藏下拉框控件（FavComboBox）
│   │
│   └── scripts/                  # 【构建 / 安装脚本】
│       ├── generate_icon.py      # 生成应用图标（PIL）
│       └── *.spec                # PyInstaller 打包规格
│
├── feature_intro/                # 功能介绍文档（HTML / PNG / MD）
│   ├── *.html                    # 功能介绍页
│   ├── *.png                     # 截图
│   └── *.md                      # 各模块说明（含 md5-check / json-tool / timestamp 等）
│
├── build/                        # PyInstaller 构建中间产物
├── dist/                         # 打包输出
└── resources/                    # 资源文件
```

## 模块职责速查

| 目录 / 文件 | 职责 |
|---|---|
| `Super_ADB_Main.py` | 主窗口、无边框拖拽、托盘、所有按钮信号接线、子目录 `sys.path` 注入 |
| `adb_utils.py` | ADB 命令封装：设备列表、shell 执行、文件推拉、包管理 |
| `ui/Super_ADB.ui` → `Super_ADB.py` | UI 定义与自动生成代码（改 UI 必须走 uic） |
| `dialogs/` | 各独立弹窗，复用窗口模式（重复点击 `raise_` 置顶） |
| `pages/` | 嵌入主窗口的选项卡页面（日志 / 文件 / 截图） |
| `monitors/` | 后台线程采样的性能监控，ScrollChart 自绘图表 |
| `utils/` | 无 UI 依赖的纯逻辑模块（解码 / 解析 / 控件） |
| `scripts/` | 不参与运行的构建/打包脚本 |

## 依赖关系要点

- 所有模块**扁平裸 import**，无包结构；`Super_ADB_Main.py` 启动时把 `dialogs/ pages/ monitors/ utils/` 加入 `sys.path`。
- 高引用文件留在根目录（`界面样式.py` / `popup_style.py` / `adb_utils.py` / `png_rc.py`），避免大规模改 import。
- `Super_ADB.py` 与 `png_rc.py` 由工具链生成，**禁止手改**。
