# Super_ADB 项目结构图

> 更新于 2026-08-21。基于实际目录核对整理，**本次更新纳入 2026-08-13 起的全部改动**：scrcpy 投屏功能 + 参数设置弹窗、桌面宠物子模块、Super_ADB_MAC 子项目（macOS 兼容适配）、三类 工具 新增（JSON读写 / 图表JS / WiFi密码破解）、统一 hooks/ 与 脚本/ 边界；**并纳入 2026-08-20～08-21 的主题系统修复**：弹窗边框/发光改为跟随主题（设备性能监控 / TCPDump对话框 / 关于对话框 改用 `popup_style.add_green_glow` + 主题 `accent`）、主窗口改为透明背景 + 4px 主题色实色边框（`WA_TranslucentBackground` + `paintEvent` alpha=1 底衬）、`_propagate_theme_to_dialogs` 的 `_parse_rgb` NameError 修复；另**复核发现** `MD5对话框.py` 第 27 行仍为无条件 `import winreg`（macOS 硬崩，修复尚未落地，见 `macOS_compatibility_plan.md` B1）。

## 目录树

```
Super_ADB/
├── README.md                      # 项目说明文档
├── requirements.txt               # 运行依赖（PySide6 / Pillow / segno / zeroconf / ifaddr）
├── 项目结构图.md                  # 本文件
├── 依赖关系图.md                  # 模块依赖图（mermaid，AST 静态扫描）
├── LICENSE                        # 许可证
├── macOS_compatibility_plan.md    # macOS 兼容改造计划（已部分落地，见 .workbuddy/automations 历史）
├── .gitignore                     # 忽略规则
├── .idea/                         # IDE（PyCharm 等）配置（inspectionProfiles / vcs / workspace）
│
├── ui/                            # UI 设计源文件
│   ├── Super_ADB.ui              # 主窗口布局（Qt Designer）
│   ├── png.qrc                    # 图标资源定义（⚠️仅保留应用图标）
│   ├── Super_ADB.png             # 应用图标源（512×512）
│   ├── Super_ADB.ico             # 打包用图标
│   ├── icon_preview.png          # 图标预览
│   └── 公众号.jpg / 透明公众号.png # 公众号二维码源素材（⚠️运行时从 Super_ADB_Main/resources/ 磁盘加载，不进资源内存）
│
├── Super_ADB_Main/               # 主程序目录（同时是 PyInstaller 构建根）
│   ├── Super_ADB_Main.py         # 主入口 / 主窗口逻辑 / 信号接线
│   │                             #   （⚠️重型子模块改为「用到才 import」，启动不全量加载）
│   ├── Super_ADB.py              # uic 从 .ui 自动生成（⚠️勿手改）
│   ├── smoke_test.py             # 冻结 exe 启动冒烟测试（offscreen 下确认不崩溃）
│   ├── smoke_stdout.log          # 冒烟测试输出日志
│   ├── scrcpy_settings_dialog.py # scrcpy 投屏参数对话框（分辨率/码率/帧率/编码/渲染驱动/QSettings 持久化）
│   ├── desk_cat.py               # 桌面宠物小猫（主窗口内悬浮、躲避鼠标；2026-08-13 衍生）
│   ├── 界面样式.py                # 深色主题样式表（STYLE_SHEET / FONT_FAMILY / 多主题字典）
│   ├── popup_style.py            # 弹窗高亮样式（add_green_glow / HIGHLIGHT_CARD_STYLE / DropArea）
│   ├── adb_utils.py              # ADB 命令封装层（AdbHelper/AdbDeviceOps/AdbFileManager + readonly_guidance + push_stream）
│   ├── adb_shell_config.json     # 配置（窗口几何 / shell 命令 / 持久化主题）
│   ├── png_rc.py                 # 编译后资源文件（⚠️已重编译：614KB → 55KB，勿手改）
│   ├── resources/                # 运行时磁盘加载资源
│   │   ├── 公众号.jpg
│   │   └── 透明公众号.png         # 关于弹窗二维码，运行时按需从磁盘读取（不占资源内存）
│   │
│   ├── data/                     # 🔧 二进制随包（打包时条件 --add-data；不存在时不打包）
│   │   ├── adb/                  # 可选：捆绑 adb 二进制（macOS 适配时需要，避免用户自备）
│   │   └── scrcpy/               # 投屏二进制（按平台探测 scrcpy-<platform>-vX.Y）
│   │       ├── scrcpy-win64-v4.1/# Windows 投屏（嵌套一层 scrcpy 目录也可识别）
│   │       └── scrcpy-mac-v2.6/  # macOS 投屏（可选）
│   │
│   ├── 对话框/                  # 【弹窗模块】⚠️全部「打开时才 import」
│   │   ├── 关于对话框.py       # 关于（二维码 / 版本 / 反馈；二维码从磁盘加载）
│   │   ├── 安装解包对话框.py  # 安装 / 解包（拖入 APK·ZIP 预览 + adb install + 安装进度三阶段 + 主题化卡片）
│   │   ├── JSON工具对话框.py   # JSON 工具（格式化/压缩/差异/YAML/Schema/树/字典互转 Tab）
│   │   ├── MD5对话框.py         # MD5 多算法校验 + 进度 + 导出 + 右键集成 + PEM subject-hash
│   │   ├── 时间戳对话框.py   # 时间戳 ↔ 北京时间 转换
│   │   ├── 哈希上下文菜单.py   # 右键「计算哈希」独立入口（独立进程 --hash 调用）
│   │   ├── Monkey压测窗口.py # Monkey 压测窗口
│   │   ├── 无线调试对话框.py # 无线调试主窗口（聚合下列子模块）
│   │   ├── 局域网扫描对话框.py  # 局域网 ADB 设备扫描（同网段/自定义范围，后台并发）
│   │   ├── 二维码连接页.py     # 二维码连接页（扫码填回 + 生成配对二维码，嵌入 QTabWidget）
│   │   ├── WiFi配对对话框.py    # WiFi 配对连接（Android 11+ 配对码流程，adb pair/connect）
│   │   ├── WiFi历史对话框.py # WiFi 配对/连接历史（表格 + 导出 CSV/JSON）
│   │   ├── WiFi对话框.py         # Wi-Fi 调试引导（依赖 WiFi工具，读本机 WiFi 配置）
│   │   └── TCPDump对话框.py      # tcpdump 抓包窗口
│   │
│   ├── 页面/                    # 【主窗口子页面】（file_manager/log_viewer 启动即建）
│   │   ├── log_viewer_page.py    # 日志抓取（QProcess 流式 logcat）
│   │   ├── file_manager_page.py  # 文件管理（右键「授权 777」+ 权限列刷新）
│   │   └── capture_screenshot.py # 截图（PIL.ImageGrab）
│   │
│   ├── 监控/                 # 【性能监控】⚠️改为「打开监控窗口时才 import」
│   │   ├── 设备性能监控.py # 设备级监控（独立窗口 + 采样线程，提供 ScrollChart 基类）
│   │   └── 应用性能监控.py    # 应用级监控（继承 设备性能监控.ScrollChart；12 项图表指标 / 泄漏 / ANR / OOM / 报告）
│   │
│   ├── 工具/                    # 【工具 / 纯逻辑模块】（无 UI 依赖，可独立运行）
│   │   ├── AXML解码器.py       # Android Binary XML 解码
│   │   ├── 证书解析器.py        # 证书解析
│   │   ├── 收藏下拉框.py          # 可收藏下拉框控件（FavComboBox，被 Super_ADB.py import）
│   │   ├── WiFi工具.py         # 本机 WiFi 配置/密码读取（Windows netsh wlan，不依赖 Qt）
│   │   ├── APK分析器.py       # 零依赖 APK 静态分析（结构/DEX/AXML/危险API/签名）离线 CLI
│   │   ├── DEX分析.py        # DEX/AXML 离线解析脚本（字符串/危险API/权限组件）
│   │   ├── 清单解析.py     # AXML 权限 & 组件最小解析脚本（离线 CLI）
│   │   ├── JSON读写.py            # 统一 JSON 读写封装（原子写 + ensure_ascii=False + 警告日志）
│   │   ├── 图表JS.py           # 本地加载 chart.umd.min.js（HTML 性能报告内联，避免依赖公网 CDN）
│   │   └── WiFi密码破解.py   # 独立 CLI：WPA PMKID 模式密码强度测试 + 本机 WiFi 密码恢复（multiprocessing + threading 双级并行）
│   │
│   ├── 打包/                    # 【PyInstaller 打包专用目录】（仅供打包，不进运行时 sys.path）
│   │   ├── Super_ADB.spec / qrtest.spec # 打包规格（主 / 二维码测试）
│   │   ├── 精简打包exe.py        # 跨平台打包脚本（Windows/macOS，向 pathex 注入子目录；macOS 输出 .app）
│   │   ├── trim_qt.py            # 构建后裁剪冗余 Qt6 DLL / 插件 / .qm（基于依赖闭包，带安全闸门；macOS 由 darwin 分支跳过）
│   │   ├── hooks/                # PyInstaller 打包钩子（收集/重定向 pyzbar 的 zbar DLL）
│   │   │   ├── hook-pyzbar.py    # 收集 pyzbar 预编译 DLL（避免打包后扫码崩溃；macOS 需 brew install zbar）
│   │   │   └── runtime_pyzbar.py # 运行时重定向 zbar DLL 加载路径（onennder/onefile 自适应）
│   │   ├── dist/                 # ⚠️打包输出目录（pyinstaller --distpath 指向此处；构建产物 Super_ADB/ 在此）
│   │   └── build/                # ⚠️PyInstaller 构建中间产物（--workpath 指向此处）
│   │
│   └── 脚本/                  # 【构建/验证/测试脚本】（不参与打包运行）
│       ├── 生成依赖关系图.py     # 依赖关系图生成脚本（AST 静态分析 import 关系）
│       ├── 生成图标.py      # 生成应用图标（PIL）
│       ├── 内存追踪.py          # 启动内存实测脚本（P2：量化延迟 import 收益）
│       ├── 测试真实窗口几何.py    # 窗口几何实测
│       ├── 测试几何持久化.py   # 窗口几何持久化测试
│       ├── 测试JSON树美化.py # JSON 树美化测试
│       ├── 测试JSON树反转.py  # JSON 树反转定位测试
│       ├── 测试局域网异步连接.py  # 局域网异步连接单元测试（FakeRun 注入）
│       └── 测试局域网扫描升级.py# 局域网扫描升级测试
│
├── feature_intro/                # 功能介绍文档（HTML / PNG / MD）
│   ├── index.html / main.html / install.html / monkey.html / perf_app.html / perf_device.html
│   ├── *.png                     # 各功能截图（main/install/monkey/perf_*/system-ops/wireless-debug-* 等）
│   ├── README.md / 功能增强总览.md
│   └── *-guide.md                # 各模块说明（file-manager / log-viewer / md5-check / json-tool /
│                                 #   timestamp / wireless-debug / tcpdump / monkey-runner / system-ops /
│                                 #   proxy / input-text / file-hash-check-v2 / app-perf-monitor /
│                                 #   device-perf-monitor / install-extract 等）
│
├── generated-videos/             # 介绍视频产物
│   └── segments/                 # 视频分镜片段（main_window_highlighted.png + boxes.txt）
│
├── Super_ADB_MAC/                # macOS 适配子项目（独立的工作副本，不进父仓库 PyInstaller 打包）
│   ├── Super_ADB/                # 完整的 macOS 适配版本
│   │   ├── Super_ADB_Main/       # 含 macOS 分支修正后的源码
│   │   ├── ui/  feature_intro/  说明/
│   │   ├── README.md  项目结构图.md  依赖关系图.md  macOS_compatibility_plan.md
│   │   └── （与父仓库结构一一对应，部分文件带 ._ 前缀为 macOS Finder 元数据，可忽略）
│   └── __MACOSX/                 # macOS 压缩包元数据（DS_Store / ._* 资源文件）
│
└── （注：PyInstaller 构建产物 dist / build 已收敛到 Super_ADB_Main/打包/ 下，见「打包/」节点）
```

## 模块职责速查

| 目录 / 文件 | 职责 |
|---|---|
| `Super_ADB_Main.py` | 主窗口、无边框拖拽、托盘、所有按钮信号接线、子目录 `sys.path` 注入、`_propagate_theme_to_dialogs` 主题切换 |
| `scrcpy_settings_dialog.py` | scrcpy 投屏参数（分辨率/码率/帧率/编码/渲染驱动/关屏）可调，`QSettings` 持久化 |
| `desk_cat.py` | 桌面宠物小猫（主窗口内悬浮子控件，自动躲避鼠标 + 气泡互动） |
| `adb_utils.py` | ADB 命令封装：设备列表、shell 执行、文件推拉、包管理、只读分区解锁引导、push_stream 进度推送 |
| `ui/Super_ADB.ui` → `Super_ADB.py` | UI 定义与自动生成代码（改 UI 必须走 uic） |
| `data/scrcpy/` | 投屏二进制按平台探测（`scrcpy-<platform>-vX.Y`）；Windows 直接 `data/scrcpy-win64-v4.1/`，嵌套 `data/scrcpy/scrcpy-win64-v4.1/` 也可识别 |
| `对话框/` | 各独立弹窗，复用窗口模式（重复点击 `raise_` 置顶）；**启动不加载，用到才 import** |
| `对话框/无线调试对话框.py` | 无线调试总入口，聚合局域网扫描 / 二维码连接 / WiFi 配对三个子模块 |
| `工具/JSON读写.py` | 统一 JSON 读写（原子写 + 异常 warning），替代散落的 bare-except 模式 |
| `工具/图表JS.py` | 本地加载 chart.umd.min.js（HTML 性能报告内联，避免 CDN 依赖） |
| `工具/WiFi密码破解.py` | 独立 CLI：WPA PMKID 密码强度自测 + netsh WiFi 密码恢复（multiprocessing + threading 双级并行） |
| `工具/WiFi工具.py` | 纯逻辑：读取本机已保存 WiFi 配置及密码（netsh wlan），供 `WiFi对话框` 调用 |
| `工具/APK分析器.py` `DEX分析.py` `清单解析.py` | 离线 APK/DEX/AXML 静态分析脚本（零依赖，可命令行直接跑） |
| `页面/` | 嵌入主窗口的静态分屏页面（日志 / 文件 / 截图），核心页面启动即建 |
| `监控/` | 后台线程采样的性能监控，`设备性能监控.ScrollChart` 是基类，`应用性能监控` 继承复用；**启动不加载，用到才 import** |
| `打包/hooks/` | 仅供 PyInstaller 打包：收集/重定向 pyzbar 的 zbar DLL，避免冻结后扫码崩溃 |
| `脚本/` | 不参与运行的构建/验证/测试脚本（含 `生成依赖关系图.py` AST 扫描 + 内存追踪.py 内存实测 + 各专项测试） |
| `打包/精简打包exe.py` / `打包/trim_qt.py` | 跨平台打包入口（Windows + macOS darwin 分支）/ 构建后 Windows Qt6 体积裁剪 |
| `Super_ADB_MAC/` | macOS 适配子项目（独立工作副本，2026-08-13 推送过）；与父仓库结构一一对应 |

## 依赖关系要点

- 所有模块**扁平裸 import**，无包结构；`Super_ADB_Main.py` 启动时把 `对话框/ 页面/ 监控/ 工具/` 加入 `sys.path`（`打包/`（含 hooks）仅供打包、`脚本/` 仅供测试，不进运行时路径）。
- **启动加载策略（2026-08-09 优化后，2026-08-20 维持）**：顶层只保留 `file_manager_page` / `log_viewer_page` / `popup_style` / `desk_cat` / `scrcpy_settings_dialog` 及核心依赖；原重型模块（`设备性能监控` / `Monkey压测窗口` / `应用性能监控` / `安装解包对话框` / `关于对话框` / `JSON工具对话框` / `MD5对话框` / `时间戳对话框` / `无线调试对话框` / `WiFi对话框` / `TCPDump对话框` / `哈希上下文菜单`）改为在对应 `open_xxx` 方法内局部 import。**启动阶段整体不加载任何弹窗/监控/扫描模块**（实测 import 主窗口后这些模块均不在 `sys.modules`）。
- **跨层穿透**（继承/复用关系）：`应用性能监控 → 设备性能监控`（继承 ScrollChart 基类）、`哈希上下文菜单 → MD5对话框`（共用算法注册表）。这两个跨层边是真实设计，不是冗余。
- 高引用文件留在根目录（`界面样式.py` / `popup_style.py` / `adb_utils.py` / `png_rc.py`），避免大规模改 import；`工具/JSON读写.py` / `工具/图表JS.py` 也加入高被引用层（4+ 入度）。
- `Super_ADB.py`、`png_rc.py`、`*.spec` 相关生成物**禁止手改**；改 UI 走 uic，改资源走 `png.qrc` + 重编译。
- 运行依赖（`requirements.txt`，2026-08-20 实际）：`PySide6` / `Pillow` / `segno`（生成无线调试配对二维码）/ `zeroconf`（mDNS 监听，实现手机扫码配对）/ `ifaddr`（本机 IP 探测，供局域网扫描）。另：`pyzbar`（二维码扫码解码，替代原 OpenCV）由 `打包/精简打包exe.py` 的 `hidden-import` + `打包/hooks/hook-pyzbar.py` 在打包时引入，`requirements.txt` 未显式声明，运行环境需另行安装（打包产物已自带 `libzbar-64.dll`）。

## 启动内存优化（2026-08-09 起）

| 级别 | 优化项 | 做法 | 实测收益 |
|---|---|---|---|
| P0 | 延迟 import 重型子模块 | 13 个弹窗/监控模块由顶层 import 改为 `open_xxx` 内局部 import | import 主窗口阶段省下 ~16.9 MB；启动不再加载 matplotlib/numpy 等重型依赖 |
| P0 | 资源文件瘦身 | `ui/png.qrc` 仅保留应用图标，公众号二维码移出资源、改为运行时从 `Super_ADB_Main/resources/公众号.jpg` 磁盘加载；重编译 `png_rc.py` | `png_rc.py` 614 KB → 55 KB；公众号图片字节不再常驻资源内存 |
| P1 | 收敛线程池上限 | `QThreadPool().setMaxThreadCount(6)` → `min(cpu_count, 4)` | 每线程约 1 MB 栈常驻，省 ~2 MB，后台任务并发仍够用 |
| P2 | 内存实测脚本 | `Super_ADB_Main/脚本/内存追踪.py`（offscreen + tracemalloc + RSS 分阶段测量） | 量化上述优化、便于回归对比 |
| P0 | 中文输入主线程阻塞（2026-08-19） | `Super_ADB_Main.py` 新增 `_TextSender(QObject)` + QThread，把 adb keyboard / 原生剪贴板输入搬出主线程 | 文本输入不再冻结 GUI；剪贴板读取主线程、写回 ctypes 直接走 Win32（绕过 Qt clipboard 线程限制）|
| P0 | 录屏 PIPE 死锁（2026-08-19） | `adb_utils.screen_record` 改 `stdout/stderr=DEVNULL`，`wait(timeout=10)` 失败改 `kill()` | 大体积录屏不再卡死 |

> 说明：PySide6 框架本身的 shiboken 绑定层（加载整个 Qt 符号）属固有常驻开销（150–400 MB），不在本项目可优化范围内；本次优化针对「项目自身」的可降占用。实测（`内存追踪.py`，无设备 offscreen）主窗口构造后 RSS ≈ 83.6 MB，重型子模块改按需加载后启动阶段不再额外占用约 16.9 MB。

## macOS / Linux 适配现状

- `adb_utils.py` / `界面样式.py` / `Super_ADB_Main.py` / `打包/精简打包exe.py` 已埋 `darwin/linux/win32` 平台分支；`Super_ADB_MAC/` 子项目是 macOS 适配的工作副本（2026-08-13 已随 commit `e5f1d2a` 推送到 `super_adb.git/master`）。
- 2026-08-20 曾记录「`MD5对话框.py` 加 `try/except ImportError: winreg = None`（原硬崩已修）」——**更正**：截至 2026-08-21，`MD5对话框.py` 第 27 行仍为无条件 `import winreg`，macOS 打开 MD5 弹窗即硬崩；该修复**实际尚未落地**，与 `macOS_compatibility_plan.md` B1 一致，仍是 P0 阻断。`打包/精简打包exe.py` 已在 `sys.platform == 'darwin'` 下生成 `.app` 并条件打包 `data/scrcpy/`；`adb_utils._config_path` 在 darwin 冻结态走 `~/Library/Application Support/Super_ADB/`。
- 剩余阻断点详见 [`macOS_compatibility_plan.md`](macOS_compatibility_plan.md)；其中已验证的小米电视 DRM 黑屏问题不受代码层面影响（scrcpy 限制，按钮 tooltip 已告知）。

## 返回 README

详见 [`README.md`](README.md)。
